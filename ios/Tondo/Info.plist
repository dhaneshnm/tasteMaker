<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>CFBundleDevelopmentRegion</key>
	<string>$(DEVELOPMENT_LANGUAGE)</string>
	<key>CFBundleDisplayName</key>
	<string>Tondo</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleName</key>
	<string>$(PRODUCT_NAME)</string>
	<key>CFBundlePackageType</key>
	<string>APPL</string>
	<key>CFBundleShortVersionString</key>
	<string>$(MARKETING_VERSION)</string>
	<key>CFBundleVersion</key>
	<string>$(CURRENT_PROJECT_VERSION)</string>

	<!-- Export compliance, answered once here instead of on every TestFlight
	     upload. The shell's only cryptography is HTTPS through the system's own
	     stack, which is exempt under the standard exemption, so this is `false`
	     as a matter of fact. Omitting the key does not mean "no" — it means App
	     Store Connect blocks each build behind a manual questionnaire. -->
	<key>ITSAppUsesNonExemptEncryption</key>
	<false/>

	<!-- Where the shell points. Set per-configuration in Config/*.xcconfig so a
	     start URL is never hardcoded in Swift. Endpoint.swift reads this key and
	     traps loudly if it is missing or unparseable. -->
	<key>TondoURL</key>
	<string>$(TONDO_URL)</string>

	<!-- The device-registration secret (story 0015). Set per-configuration in
	     Config/*.xcconfig like the URL. Extractable from the IPA by anyone who
	     unpacks it — decisions/0011 accepts that: it stops open/anonymous
	     access, not reverse engineering. -->
	<key>TondoAppSecret</key>
	<string>$(TONDO_APP_SECRET)</string>

	<!-- The callback scheme the auth sheet comes home on (story 0017 Release 2).
	     `ASWebAuthenticationSession` refuses a callbackURLScheme the app has not
	     claimed here, so this is not optional bookkeeping — without it the sheet
	     opens and never returns.

	     Used for exactly one URL: `tondo://auth?handoff=<token>`, the redirect
	     the OAuth callback issues instead of writing a session it cannot reach.
	     The token is single-use and expires in 60 seconds, which is what makes
	     the scheme safe to share: on iOS any app may claim any scheme, so a
	     hijacker would win a race for a credential that dies on first use. -->
	<key>CFBundleURLTypes</key>
	<array>
		<dict>
			<key>CFBundleURLName</key>
			<string>$(PRODUCT_BUNDLE_IDENTIFIER).auth</string>
			<key>CFBundleTypeRole</key>
			<string>None</string>
			<key>CFBundleURLSchemes</key>
			<array>
				<string>tondo</string>
			</array>
		</dict>
	</array>

	<!-- Local networking only. This permits http://localhost:3000 during
	     development without opening arbitrary cleartext to the internet, so it
	     is safe to ship as-is. -->
	<key>NSAppTransportSecurity</key>
	<dict>
		<key>NSAllowsLocalNetworking</key>
		<true/>
	</dict>

	<!-- The plain launch-screen dictionary: a background colour and a centred
	     image. No storyboard, and no fonts needed at launch — the wordmark is
	     shipped as an asset. The first frame of the product is linen, not the
	     white default flash, because the calm starts before the app opens. -->
	<key>UILaunchScreen</key>
	<dict>
		<key>UIColorName</key>
		<string>Linen</string>
		<key>UIImageName</key>
		<string>LaunchWordmark</string>
		<key>UIImageRespectsSafeAreaInsets</key>
		<true/>
	</dict>

	<!-- The error view is the one screen the web cannot draw, because by
	     definition there is no network when it appears. These are static
	     instances cut from the same latin subsets the website uses, at the two
	     sizes the empty state calls for — iOS cannot load woff2, and shipping
	     the full variable families would be ~1.5MB for two lines of text.
	     SIL OFL 1.1; the licence text ships beside them in Fonts/. -->
	<key>UIAppFonts</key>
	<array>
		<string>Fraunces-Italic.ttf</string>
		<string>Newsreader-Regular.ttf</string>
	</array>

	<key>UIApplicationSceneManifest</key>
	<dict>
		<key>UIApplicationSupportsMultipleScenes</key>
		<false/>
		<key>UISceneConfigurations</key>
		<dict>
			<key>UIWindowSceneSessionRoleApplication</key>
			<array>
				<dict>
					<key>UISceneConfigurationName</key>
					<string>Default Configuration</string>
					<key>UISceneDelegateClassName</key>
					<string>$(PRODUCT_MODULE_NAME).SceneDelegate</string>
				</dict>
			</array>
		</dict>
	</dict>

	<!-- Landscape is supported, which is why the stylesheet carries left/right
	     safe-area insets. Upside-down is not: nothing in this product wants it. -->
	<key>UISupportedInterfaceOrientations</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
		<string>UIInterfaceOrientationLandscapeLeft</string>
		<string>UIInterfaceOrientationLandscapeRight</string>
	</array>

	<!-- The shell forces .light in code (decisions/0006). This is the belt to
	     that braces: it stops the system from flipping native surfaces to black
	     before any of our code runs. -->
	<key>UIUserInterfaceStyle</key>
	<string>Light</string>

	<!-- `arm64`, not the `armv7` the old Xcode templates emitted. armv7 is
	     32-bit, no device running iOS 17 has it, and declaring it as *required*
	     describes a phone that cannot exist. -->
	<key>UIRequiredDeviceCapabilities</key>
	<array>
		<string>arm64</string>
	</array>
</dict>
</plist>
