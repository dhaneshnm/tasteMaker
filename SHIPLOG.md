# SHIPLOG

Every ship and publish gets a line with a receipt (link, commit, screenshot). A week with
shipped code and zero initiated user contact is builder's gravity — it gets named here.

| Date | What shipped / published | Receipt |
|------|--------------------------|---------|
| 2026-07-24 | Infinite-scroll MVP committed (pre-BET; input metric only — see R7) | fdf28c5 |
| 2026-08-03 | BET.md completed; personas.md built from 31 DailyArt reviews | 286c068, 1dffea4 |
| 2026-08-03 | Story 0001 "Artwork of the Day" built — baseline item 1. Code only, **not published, not deployed, zero users, zero installs**: an input metric under R7. | 4221f55 |
| 2026-08-04 | Story 0007 "no session cookie on public pages" built — a live defect on already-shipped pages, split out of 0006 at eng review. Code only. | 6747990 |
| 2026-08-04 | Story 0006 "favorites" built — **baseline item 4**, the last purely-web baseline item. Design review 6/10 → 9/10 (9 decisions), eng review 8 findings, QA 2 findings / 1 fixed. Code only, **not published, not deployed, zero users, zero installs**: an input metric under R7. | 9b22661, d2e9f7c, ae229ef |
| 2026-08-05 | Story 0004 "the page that isn't there" built — the 404 renders on linen and keeps its status code. Code only. | eeb9946 |
| 2026-08-05 | Kamal + Thruster deploy config written and rehearsed against a real production container. **Still not deployed** — needs a VPS. | 244254f |
| 2026-08-07 | Story 0009 "the typefaces come from us" built — Fraunces and Newsreader self-hosted, two third-party hosts off the critical path. Code only. | 5a6929c |
| 2026-08-07 | Story 0008 "the shell that gets it onto a phone" built — **baseline item 2's precondition**, a Hotwire Native iOS target that builds and runs on an iPhone 17 simulator against `localhost:3000`. Code only, **not on a device, not on TestFlight, not submitted, zero installs**: an input metric under R7. | 39e68c7, 6861b81 |

| 2026-08-10 | Bug fix, not a story: shipping story 0009's CSS moved the asset digest but not the ETag, so any browser holding the previous HTML revalidated into a 304 and kept linking a stylesheet URL that no longer resolved — the page rendered unstyled and did not heal on reload. Found by hitting it locally, not by a test. Code only. | ae742dc |
| 2026-08-13 | Stories 0011 "rename to Tondo" and 0012 "getting around", in one commit — the tree never had a 0011-only state, so splitting them would have mislabelled one story's work as the other's. 0011: the product is Tondo, a rose-window mark, one masthead partial where six copies used to be. 0012: a compass on every screen and a sticky rail on `/feed`, which was a dead end — its only exit was a wordmark that had scrolled off the top. Design review 6/10 → 9/10 (4 decisions), eng review 12 findings, Codex outside voice 9 findings (4 novel, all confirmed). Code only, **not published, not deployed, zero users, zero installs**: an input metric under R7. | bea621c |

| 2026-08-13 | Story 0013 "a deeper pool" built — the pool goes from **110 works in one museum to 2,000 across four** (Met, Art Institute of Chicago, Cleveland, Minneapolis). Selection is a quota table in code, not a hand-pick: per-artist ceiling 5, no museum over 50%, no region over 25%, 53% outside Europe and North America, 18% dated 1900+, 83% carrying readable museum text, every plate \u2265 1600px. `db/seeds/pool_report.md` is the receipt; `test/lib/pool_quota_test.rb` fails the build if a reseed regresses any bar. 0.94 GB on disk against CX22's 40 GB. Code only, **not published, not deployed, zero users, zero installs**: an input metric under R7. | c770303 |
| 2026-08-13 | Fix, reported from the running app: the front door read "This work is resting" where the artwork should have been. `Primary_RenditionNumber` carries a suffix (`mia_3003333_001.jpg`) and the regex reading it dropped it, so 43 works downloaded blank and 57 more were wrongly vetoed as unreachable. One of the blank ones had already been published. Dead plates 77 → 20; Minneapolis mirror 1,547 → 1,670. 2,002 rows, 2,002 images. | 1e79c05 |
| 2026-08-14 | Story 0014 "the action rail" built — reader actions leave the bottom of the wall label and become a row of line icons under the plate, because `Keep this` (the habit mechanic, one of two named moats) was measured 40–60pt below the fold on the front door with nothing on screen saying so. Amends `DESIGN.md` rule 6 via `decisions/0010`. QA found ISSUE-001, the rail's zoom control sharing the plate's accessible name; fixed with a regression test. **Logged late** — this shipped in five commits and sat unlogged while the session moved on to hosting. | a4ea62b, 2dd8ca3, e3d977c, 9e298be, a34fd48 |
| 2026-08-14 | **Tondo is live: https://dailytondo.com** — the first receipt in this file that is a URL instead of a commit. One GCP e2-medium in us-central1, Ubuntu 24.04, `kamal setup` exit 0 in 1,171s, valid Let's Encrypt cert, HTTP 301 → HTTPS, `/up` 200. Domain bought and DNS cut over the same day; USPTO Class 9/41 search run and clear before the money was spent. **Nobody has opened it, there is no daily pick scheduled, and the front door reads "The first artwork arrives soon."** | https://dailytondo.com, d292302, b092833, 096c586 |

**The day the URL started existing, and what that is worth (R7), 2026-08-14.** For the
first time in this log the receipt column holds an address instead of a hash. That is the
precondition for every threshold in `BET.md` and it is **not progress against any of them**.
The scoreboard is unchanged: **0 of 4 posts, 0 of 3 keywords, 0 of 5 user conversations, 0 of
50 installs**, and "app live on the App Store by **Aug 14**" is now **missed rather than at
risk** — that was today.

Three hosts were named in two days before one existed. `decisions/0009` chose a Hetzner CX22
in Ashburn; it is not sold there — the CX line is Germany/Finland only — and the €4.49 it
quoted was already two months stale, Hetzner having raised USA CPX pricing ~3× on
2026-06-15. Both were checkable the day 0009 was written. `decisions/0012` supersedes it with
GCP and says plainly that the choice was made on **speed, not price**: the spread between the
cheapest and dearest option, across the entire remaining life of this bet, is **$7.76**. That
$7.76 consumed a decision entry, a price correction, a cost estimate and most of a session.

**Two defects that only production could find.** The first: `bin/docker-entrypoint` runs
`db:prepare`, which seeds on a fresh database, which downloads 2,000 images before the app
answers — against Kamal's default `deploy_timeout` of **30 seconds**. Caught by reading the
entrypoint before deploying rather than by watching a rollback; the failure would have looked
like a broken app instead of an impatient timeout.

The second is the more useful one. The seed stored **1,780 of 2,000** plates. Every one of
the 220 failures was the Art Institute — 220 AIC works in the pool, 220 failures, zero AIC
images. `lib/pool/sources.rb` had already paid for both lessons (no email address in the
User-Agent, and AIC's Cloudflare wants its own `AIC-User-Agent` header) and `db/seeds.rb`
carried a **duplicate copy of the string** that had learned neither. Fixed by deleting the
duplicate rather than syncing it. What makes it worth writing down: from a residential IP the
old headers work fine and from the datacenter they 403, so this bug was **invisible to every
local test and certain in production**. The first stated diagnosis was contradicted by the
first test and only held up when run on the box.

**Still owed before a single external reader**, unchanged and now overdue rather than
upcoming: no backups, no logged restore test, no error tracking, no analytics — session gate
6, against a SQLite file on one disk. A VM snapshot of a live SQLite file can capture a torn
WAL and is not a restorable backup. Choosing a third host in two days did not make that work
smaller.

| 2026-08-14 | Story 0015 "the two keys" built — every reader-facing endpoint now answers only a signed-in web session (Google/Apple OAuth, no passwords) or a registered device (Keychain UUID → signed cookie); everything else bounces to the landing page's sign-in anchor. The landing page stays public and byte-identical across all three identity states. Design review triage 5/10 → 8/10 (3 decisions), eng review 19 findings + Codex outside voice (19 points, 11 folded), 4-agent simplify (17 applied), 2-agent code review that caught a real ship-blocker: Apple's cross-site POST callback would have 422'd on CSRF in production while the suite stayed green. **The Apple flow is still unverified end-to-end — it cannot be exercised off a deployed domain, and there is no deployed domain.** Code only, **not published, not deployed, zero users, zero installs**: an input metric under R7. | 74c4516, 5db1dfb, b5301d1 |

| 2026-08-16 | **Story 0015's first door actually opens in production.** The sign-in buttons had been live on dailytondo.com for two days and neither one worked: the ship checklist's provider setup was never run, so both strategies booted with blank client ids and every tap died at Google's door. Google web client created, consent screen published (non-sensitive scopes, no verification review), credentials committed, deployed. Request phase verified against Google on the live domain — real `client_id`, `redirect_uri` accepted, sign-in page returned. Shipped alongside two bug fixes found the same day: museum HTML reaching the wall label as literal `<em>` (only 1 of 4 adapters flattened it; **424 rows rewritten in prod**), and a suite that typed `ENV`'s secret while the controllers read credentials — every admin and device test had been 401'ing since `a6dba37`, invisibly, because a clone with no master.key kept passing. **Apple is still dead**: its five values are absent from credentials, so that button fails exactly as before. Two doors, one open. | ed55a8e, 184164d, 17eae52, https://dailytondo.com |

| 2026-08-16 | **Both doors open. Story 0015 works against a real Apple ID on the live domain.** Apple's Services ID, team id, key id and `.p8` written and deployed — and the flow then failed exactly where `specs/0015-the-two-keys/plan.md` said it would, twelve days after the plan said it. Apple returns the callback as a cross-site POST; `_tondo_session` is `SameSite=Lax`; the browser sends nothing; `omniauth.state` is gone. The one thing the plan did not predict was the error string: not `csrf_detected` but **`undefined method 'bytesize' for nil`**, because omniauth-oauth2's `secure_compare` compares against the nil session value with no guard. Fixed with the scoped-cookie option the plan had already chosen — a signed `/auth` cookie, `SameSite=None; Secure`, carrying state **and** nonce, spent on first use, with `_tondo_session` left at `Lax`. No fork fallback, no time-box breach. **A green suite could not have caught this and did not**: `OmniAuth.config.test_mode` skips the request phase, so the middleware is tested as Rack and a mutation run proves the test reproduces the production nil. Still an input metric: **zero users have opened either door.** | 247b613, 7472be9, https://dailytondo.com |

| 2026-08-16 | **Tondo is on a phone.** Story 0008's shell, built Aug 7, had never left the simulator; it now runs on a physical iPhone 13 Pro (iOS 26.5.2) against `https://dailytondo.com`, signed `Apple Development (7UHVJKQDCU)`, installed by `devicectl`. Three blockers had to go first, none of which raised an error: no `DEVELOPMENT_TEAM` anywhere (a simulator build never asks, a device build cannot sign), `TONDO_APP_SECRET = CHANGEME` still in `Release.xcconfig`, and `UIRequiredDeviceCapabilities` declaring `armv7` — a 32-bit capability no iOS 17 device has. The secret one is the one worth keeping: verified against production, the real value answers **204** and `CHANGEME` answers **401**, and `DeviceIdentity.register` swallows the 401 and routes anyway — so shipping the placeholder would have reached App Review as "sign-in is broken", not as a misconfiguration. Secret now generated from credentials by `script/ios-secrets` into a gitignored `Config/Secrets.xcconfig`. **One device, mine, dev-signed. Not TestFlight, not submitted, zero App Store installs**: an input metric under R7. | f025187 |

| 2026-08-18 | **Story 0017 Release 2: sign-in reaches the app, and the collection stops belonging to the phone.** Google answers an embedded web view with `403 disallowed_useragent` by policy, so OAuth cannot happen in the shell's WKWebView. `ASWebAuthenticationSession` can — but it has its own cookie jar and Apple gives no way to share it, so what crosses is a 60-second single-use token: tap → `/auth/start/:provider` matched and cancelled by a new `AuthRouteDecisionHandler` → sheet → consent → callback in **Safari's** jar → `tondo://auth?handoff=…` → `/session/handoff` in the **web view's** jar, which spends the token, writes the session, and claims. **The claim happens on the last leg because that is the only request in the flow that can see both keys at once** — the callback has no device cookie. Two corrections the build made to the plan: `native=1` had to move to the QUERY STRING, because OmniAuth stashes `request.GET` as `omniauth.params` and a POST-body field is never seen again (found by running it — the callback was redirecting to `/days`); and the doors render as LINKS for a native reader, because a form submit is not a `VisitProposal` and would sail past the handler into the 403. `generates_token_for` plus a counter column replaced the planned `handoff_tokens` table — Rails signs and expires it, so there is nothing to store or sweep. **The version gate, measured on a simulator rather than assumed**: the eng review found `applicationUserAgentPrefix` carried no version, so the server could not tell one binary from another while Rails deploys in seconds and binaries do not. Version and bridge now ship together, so presence is the whole test — instrumented the wall, booted the shell, read `Tondo iOS/1.0 → bridge=true` against an old shell's `Tondo iOS; → bridge=false`, removed the probe. Verified on production after deploy: both `/auth/start` routes 200 and `facebook` 404s on the constraint, a forged handoff answers 303 to `/you` rather than an error, a versioned shell gets two door links and an unversioned one gets zero, and `/` still answers `public, no-cache` with **no `Set-Cookie`**. Migration ran on container boot via `db:prepare`. `bin/ci` 342 runs / 1760 assertions, 48 / 293 system. **Not verified, and it needs a human**: the OAuth round trip through a real sheet against real Google credentials — `simctl` has no pointer input, so everything up to the tap is proven and the tap is not. **Still zero on every BET threshold**: the binary has never been uploaded, so no reader can reach any of this from a phone, and nothing advances the daily pick. | e4dbc42, https://dailytondo.com/auth/start/google_oauth2 |
| 2026-08-18 | **Story 0017 "Your corner", Release 1: the account surface, and the fragment the front door stopped needing.** `/you` is the fifth reader surface and the second unwalled one, reached by an oculus in the masthead on all eight screens that wear it and by a word in the front door's coda. Sign-out and both delete doors moved there; `/collection` keeps a labelled signpost so the majority iOS reader — a registered device that never signed in — did not trade a button that says what it does for a mark that does not. **The landing page's per-visitor fragment is deleted, not relocated**: `sessions#control`, its route, the `signin` frame and the `/#signin` anchor. That removes a round trip from every front-door render and takes the product's most delicate cache hazard off the one page Thruster stores. `/you` has **four** states, not three — the unregistered shell holds nothing, claims nothing, and is offered no delete button, because `DevicesController` answers one with a silent redirect when `current_device` is nil. Sign-in doors stay suppressed for the shell entirely: Google answers an embedded web view with `403 disallowed_useragent`, so they arrive with the native transport in Release 2 or not at all. **Four review passes found six things the build did not.** The wall's retarget turned out to be a detector for tests that were silently measuring the bounce target — `dynamic_type_test` had been comparing the front door with itself on its second route since story 0015 walled the archive (36 → 78 assertions), and `design_test` had the identical bug against `/feed`. Two eager frame GETs were being written over with "Content missing": the keep frame, which only ever worked because the old bounce landed on a page carrying the same frame, and `/feed`'s pagination sentinel, which the first fix missed and whose comment asserted the case was unreachable. `/simplify` found a real defect — `locked:` was derived from the view's render enum rather than the wall's predicate, so the unregistered shell rendered four live compass links that all bounced back to `/you` (measured 4 links / 0 locked; now 0 / 3). Three pre-existing defects fixed in passing: `.legal`'s two links, the nav App Review requires, were shipping at ~15px against a 44px rule; `.page--empty` lost its top padding to a compass sibling rule on every empty screen; and `privacy_claims_test` never tested a route despite the plan claiming it caught the 0016 defect. `bin/ci` 321 runs / 1682 assertions integration, 48 / 293 system, 0 failures. **Still an input metric, and the two things that are not both remain untouched**: the binary has never been uploaded — the `BET.md` live-by date was Aug 14 — and nothing advances the daily pick, so the front door holds the last published day over forever with no job and no spec. Zero of five user conversations. **Deployed to dailytondo.com** in 55.1s, no migration, and verified against production rather than the exit code: `/you` 200 where it was 404, `/session/control` 404 where it was 200, the three walled paths 303 to `/you`, and `/privacy` and `/support` still 200 for the anonymous fetches App Review makes. The two properties most able to fail silently both hold live — `/` answers `public, no-cache` with an ETag and **no `Set-Cookie`** (the story 0007 contract, which a per-visitor mark in the masthead would have broken), and `/you` under the shell's user agent with no device cookie renders **zero sign-in doors** and three locked compass words. | 9c26906, 6d0a46b, b616cf4, f71c152, 7cb3f75, 6baf3e0, 9e4fe05, https://dailytondo.com/you |
| 2026-08-16 | **Story 0016 "the listing", part one: the two pages Apple opens are live, and the exit door most readers never had is built.** `https://dailytondo.com/privacy` and `/support` answer **200 anonymously, to Safari 14, and to AppleBot** — the three fetches that decide whether App Store Connect accepts the record. `PagesController` inherits `ActionController::Base` rather than `ApplicationController`, because the planned `skip_before_action :allow_browser` **raises at class definition** (Rails installs it as an anonymous lambda) and the `raise: false` workaround is a **silent no-op**. Measured before writing the fix: Safari 14 → 406, curl → 200, empty UA → 200, AppleBot → 200, so the victim was never Apple's crawler but a reader on old hardware. `DELETE /device` is the other half: `accounts_controller.rb:7` returns early unless `current_user`, so **the default state of every iOS reader — registered device, never signed in — could not delete anything** while the policy promised in-app deletion. Also today's pick, scheduled by natural key (`cma`/`163797`) after production turned out to assign id **860** where this machine says 2691 — the front door had been reading `FRI, AUG 14` for two days. `bin/ci` 308 runs / 1614 assertions / 0 failures, 47 system tests. **Three store frames captured at 1320×2868 from the Release build against production.** Not submitted: no upload, no review, **zero App Store installs** — an input metric under R7. | dd90512, https://dailytondo.com/privacy |
| 2026-08-18 | **First real device smoke on Release 2 found the bug the plan's own "not verified, needs a human" line predicted.** Tapped Continue with Apple on a physical phone against production: the sheet signed the reader into the auth sheet's own Safari-backed jar and landed on `/days` there — the shell's `WKWebView` stayed signed out. Root cause: `lib/middleware/apple_state_cookie.rb`'s `CARRIED` list, added 2026-08-16 to survive Apple's cross-site POST callback, restored `omniauth.state` and `omniauth.nonce` but not `omniauth.params` — so the native shell's `native=1` flag never reached `SessionsController#native_auth?`, which read a dropped session as a web sign-in and wrote the session into the wrong jar instead of handing off a token. Google was never affected — its callback is a same-site GET, so the session (and `omniauth.params` with it) survives without help. `CARRIED` now carries three keys, not two; `apple_state_cookie_test.rb` gained a case that reproduces the drop directly (the same reason the original bug shipped green — `OmniAuth.config.test_mode` makes this invisible to any integration test). `bin/ci` 343 / 1761, 48 / 293 system, 0 failures. Deployed in 51s, `/up` verified 200 post-deploy. **First bug an actual device smoke caught, not a suite — the "iOS device smoke against the live host" item this repo's own memory had flagged open since 2026-08-16.** | ef846ba, https://dailytondo.com |
| 2026-08-19 | **Story 0018 "The names you know", Release 1: the artist page — artist names stop being dead ends.** `/artists/:slug` is live: one indexed lookup on a new `artist_slug` column, walled like every reader surface. Names link on `/feed` and `/days/:date`; on `/` they render **dim and unlinked**, because the front door is public and the artist page is not — an anonymous first-time reader tapping a linked name there would meet a sign-in screen instead of more work. The heading prefers the accented spelling among merged variants ("Paul Cézanne", never "Paul Cezanne"), and `Painting.artist_slug_for` deny-lists culture-as-artist strings so a name that is really a region never becomes a page. **QA browsing `/feed` found the deny-list's blind spot the reviews built it with**: "Unidentified artist" and four sibling museum placeholders (9 works) all linked to live artist pages — the list had been assembled by querying country/culture strings and never checked attribution placeholders, a different shape of the same defect. Exhaustive for the pool as measured: a regex sweep over all 1,019 distinct artist strings returns exactly those five. `/code-review` then caught a 500 waiting on a future reseed — `String#parameterize` keeps literal underscores, so `artist_slug_for` could persist a slug the route constraint rejected and `artist_path` would raise on every render of that work; confirmed reachable by direct reproduction, constraint widened. Plus a differently-cased placeholder slipping the deny-list, a curly quote able to out-vote a genuine diacritic in the accent tie-break, and a controller comment claiming one query over code measured at two (now one). Review trail: design review 17 decisions, eng review 24 findings with **scope cut** — Release 2's coverage fill split back to `IDEAS.md` after the outside voice reversed two of the eng review's own accepted decisions by catching what neither review saw: `/` linking to a walled page. Suite, rubocop, brakeman, bundler-audit, importmap audit green at ship (858fce6). **Deployed to dailytondo.com in 68.3s** — after one failed start: the registry password lives only in the operator's shell, and an agent shell without it sent `docker login` an empty `-p`. Migration + backfill ran on container boot. Verified against production, not the exit code: `/artists/:slug` anonymous → 303 `/you`, the front door's artist renders as a dim `<span>` with no anchor, `/` still answers `public, no-cache` with an ETag and **no `Set-Cookie`**, `/up` 200. **Still an input metric under R7: zero readers have tapped an artist name** — the page sits behind a wall no App Store reader can reach, because the binary has never been uploaded. | c1f21af, 6c233e6, 47f9b2d, 858fce6, https://dailytondo.com |
| 2026-08-19 | **Story 0019 "The coverage fill" built: the names a visitor knows are in the pool, and the artist page stops lying about how many works it holds.** Recognizable-name coverage of the reachable set moves **69.5% → 100%** — the 32 names four CC0 collections could supply and the pool did not hold are now in it, including **Katsushika Hokusai (16 qualifying works available, 0 shipped), Frans Hals (16), Sandro Botticelli (8), Utagawa Hiroshige (8), Canaletto (8), Johannes Vermeer (5)**. The mechanism is one new stage, `Pool::Curator#fill_recognizable`, running **first**: the quota table optimises range, era and readable text and is blind to whether anyone has heard of the artist, which is how 2,000 paintings shipped with no Vermeer and no Hokusai while both sat unused in the mirrors. It takes ≤ 3 works per name through the same `take`/`room_for?` path as every other stage, so it **cannot break a bar** — only come out incomplete, and the report prints by how much. **No TARGET growth**: the seed spec's "every net-new European work breaks `verify!`" arithmetic was true and irrelevant, because the fill substitutes recognizable Europeans for arbitrary ones inside Europe's existing 25% budget. **The matcher is the part that was easy to get wrong**: exact-slug matching finds **0** Goya works against mirrors holding **29** — no museum spells him "Francisco Goya" — so a name resolves to a *set* of slugs from its Wikidata aliases plus the museums' own parentheticals, with attribution qualifiers rejected outright ("Workshop of Titian" is not Titian) and matching exact-after-reduction so "Polidoro da Caravaggio" and "Rembrandt Peale" never reach the wrong page. **Two defects fixed that no test could see.** `artist_key` deleted accents where `Painting.artist_slug_for` transliterates them, so "Paul Cezanne" and "Paul Cézanne" were two artists to the per-artist ceiling and one to the artist page — **nine works on one page against a rule that says five**; measured 9 → 5, and `pool_quota_test` now *asserts* what story 0018 could only print (E1/X2, `decisions/0014`). And **49 works over 31 strings were shipping as live artist pages for places, not painters** — `/artists/india-calcutta` held five — found by sweeping manifest and mirrors with the `PLACES` vocabulary `Pool.region_for` already uses (102 distinct strings, 580 rows); third time this defect has been found by hand, and now it has an assertion. `user-research/0007` is the list: 200 artists ranked by 12-month English Wikipedia pageviews. **Reddit was the planned primary instrument and both archives rate-limited at 871 documents**, so the two swapped roles and the report says so in its first section. Two filters were built and thrown away before one worked — occupation blocklists deleted Leonardo, Picasso, Warhol, Dalí, Basquiat, Rembrandt and Vermeer; "any art noun anywhere" admitted Hermann Hesse and Grace Slick — and the rule that holds is positional: the earliest role noun in Wikidata's description decides. **Reviewed by a five-lens adversarial workflow** in place of `/plan-eng-review`, which is interactive and had nobody to answer it: 49 findings raised, **11 survived independent refutation**, 8 changed the work — including **a vacuous CI pass inside this story's own forcing function** (`tmp/pool` is gitignored, so an empty mirror scored a perfect 1.0 and the coverage assertion passed by measuring nothing). `bin/ci` green: rubocop, brakeman, gem and importmap audits, 413 runs / 2094 assertions, 49 system / 302, seed replant, **0 failures 0 skips**. Verified against a reseeded local database: max works on any artist page **5**, zero place-word artist pages, 349 works pruned with 2 published/favorited works protected. **Deployed to dailytondo.com in 56.0s** and verified against production rather than the exit code: `/up` 200, `/` 200 still answering `public, no-cache` with an ETag and **no `Set-Cookie`** (the story 0007 contract), the artist page 303ing anonymous readers to `/you`, and the front door's artist still rendering as a dim `<span>` with no anchor (story 0018's X3 rule, the one this re-curation could most easily have broken). **The reseed is incomplete and that is the honest state**: `bin/docker-entrypoint` runs only `db:prepare`, which seeds a *new* database, so a changed manifest needs an explicit `db:seed` on the box — it started, then the operator's shell timed out at two minutes while it downloaded the ~349 new plates. Metadata and the prune landed; images are partial, and `Painting#display_image?` falls back to the museum CDN meanwhile, so the pool renders rather than breaking. Resumable by design — it downloads only for works with no attachment. **`/code-review` then found the story's own defect class had recurred a fourth time** (`cbbac38`): the re-curated manifest put `"Anonymous"`, `"Teotihuacan"`, `"Chancay"` and `"Gujarati"` back on live artist pages, and `/artists/anonymous` was reachable. The forcing function this story added had *printed* three of them and been ignored, because the report listed 35 place-shaped strings under a header claiming each one ships a live page when **34 were already denied and shipped nothing** — finding the real one meant re-checking 34 handled strings. Filtered to still-linkable only: 35 → 1. The placeholder shape is a closed set of English words, so it is now an assertion, mutation-checked by removing `"Anonymous"` from the deny-list and watching it go red. Seven more findings fixed, of which the one that mattered: `rights` was documented in five places as a per-name hand call and was generated by the death-year rule those comments disclaim — it now consults **mirror evidence first**, correcting 12 rows including Kandinsky, Munch, Mondrian, Matisse and O'Keeffe, each labelled "in copyright, unreachable at any effort" while their works sat in the CC0 mirrors. **The manifest is unchanged** — none of it touches curation, because `artist_key` deliberately excludes the deny-list — so the report's review section was regenerated rather than the pool re-curated. **Deployed (50.8s) and seeded.** The seed also settled a question the first run left open: it reported `Downloading images for 0 paintings`, so the run that appeared to die with the operator's shell had in fact finished its downloads — **2,000 of 2,000 plates attached, 0.66 GB on disk**, down from 0.94 GB because 349 pruned works took their blobs with them. Because `db:seed` re-saves every row, `before_save` recomputed `artist_slug` under the corrected deny-list on the same pass. Verified against production: `/up` and `/` 200, `/` still `public, no-cache` with **zero `Set-Cookie` headers**, `/privacy` and `/support` 200 to the anonymous fetches App Review makes, and every `/artists/:slug` 303ing to `/you` for a reader who is not one. **The four numbers the story is judged on, read out of the production database rather than inferred**: max works on any artist page **5** (it was 9), `katsushika-hokusai` **3**, `utagawa-hiroshige` **3**, `johannes-vermeer` **3** — all three of which were 0 this morning — and `anonymous` slug rows **0**, so the page the code review found is gone from the box it was found on. **Zero of five `BET.md` thresholds move**, the binary has still never been uploaded, and no reader has yet tapped an artist name: an input metric under R7. | d2c86a6, ff67b8a, cbbac38, 22675a8, https://dailytondo.com |

**What the reviews caught that the build would not have, 2026-08-16.** Worth recording
because the ratio is unusual. `/plan-design-review` scored the plan 4/10 and the reason had
nothing to do with layout: the app's own admin queue reports **1 of 1 published day running
museum text (100%)** against `decisions/0004`'s under-20% bet, the archive held one row and
the collection was empty — so **three of the five planned screenshots could not be shot
honestly**, and the listing's strongest claim would have been contradicted by its own
screenshots. The decision was to shoot what exists and write the copy to match, which means
**the hand-written editorial claim is not in the App Store description**. That is the moat
`CLAUDE.md` names first, absent from the storefront because it is unwritten rather than
because it was declined.

The outside voice (Codex) then found four things a first-party review had already missed,
all verified before folding: guideline **5.1.1(i) requires the privacy link inside the app**,
not only in metadata — which reversed a "not in scope" call made an hour earlier and would
have been a rejection; **device-only readers had no deletion path at all**; the plan named a
**local database id** for the store hero where production assigns its own (confirmed wrong at
scheduling time, 2691 vs 860); and `public_cache_headers_test.rb` still asserted the front
door was the only public page. Two independent reviewers found the `allow_browser` defect
separately.

**What is still owed, and one of them got worse today.** `support@dailytondo.com` **does not
receive mail**, and it is now printed on two live pages telling readers they can request
deletion there — that moved from "blocks the filing" to "the published policy overstates a
channel". And **nothing advances the day**: `app/jobs/` holds only `application_job.rb`,
Solid Queue is not in the Gemfile, and `DailyPick.current` holds the last published day over
indefinitely. Today's pick was scheduled by hand; tomorrow the front door goes stale again,
under a description that says "one painting a day". Both reviewers named this as outranking
push. It still has no spec.

**Builder's gravity, named a fifth time (R7 / session gate 5), 2026-08-13.** Two more
stories built today and **still zero initiated user conversations** — the count `BET.md`
needs five of, and has none of, with eighteen days to the kill review. The App Store
submission target was ~Aug 11 and the live-by date is **tomorrow, Aug 14**. There is still
no VPS, nothing is deployed, and the App Store Connect listing — the ASO channel this whole
bet rides on — still has no spec.

What today actually produced: navigation polish on an app nobody outside this repo has
opened. It is worth noting that the day's most valuable find was not the feature — it was
that `ApplicationSystemTestCase` claimed a 375px viewport and had been running at 500px
since it was written, so every fold measurement in this product had been looser than the
phone it named. That is a real defect fixed. It is also still an input metric.

Every threshold in `BET.md` is unchanged and zero: not live, 0 of 4 posts, 0 of 3 keywords,
0 of 5 user conversations, 0 of 50 installs.

**Log gap, named rather than backfilled:** stories 0002 (feed zoom, `1ddc20b`), 0003 (past
days, `62bd3db`) and 0005 (museum note fallback, `22aec4c`) shipped without a line here. The
log was not current when this session started.

**Builder's gravity, named a third time (R7 / session gate 5), 2026-08-07.** Apple Developer
enrollment is done, which unblocks App Store Connect, TestFlight and an APNs key — and the
shell now exists to put into them. **None of that has happened.** There is still no VPS
(`config/deploy.yml` carries five `CHANGEME` values), so nothing is deployed and the Release
build points at a placeholder host. Baseline item 2 — daily push, the habit driver and the
only reason the shell was urgent — has no spec, no number, and no code; it is story 0010 and
it is not started.

Every threshold in `BET.md` is still zero: not live, 0 of 4 posts, 0 of 3 keywords, **0 of 5
user conversations**, 0 of 50 installs. The App Store deadline is Aug 14 and the submission
target was ~Aug 11, which is four days out. Nobody outside this repo has opened this app.

Two things that are not on any list and gate the bet directly: the App Store Connect listing
— name, subtitle, keyword field, screenshots, privacy labels — has no spec at all, and it
*is* the ASO channel `BET.md` bets everything on. And session gate 6 is unmet: no error
tracking, no analytics, no backup or logged restore test for a SQLite file living on a
single volume.

**Builder's gravity, named again (R7 / session gate 5), 2026-08-05.** The Proven baseline is
now 4/5 and the deploy config is rehearsed, and **every one of BET.md's five thresholds is
still zero**: not live on the App Store, 0 of 4 posts published, 0 of 3 keywords ranked, 0 of
5 user conversations, 0 of 50 installs. Nine days to the Aug 14 deadline, six to the ~Aug 11
submission. Nothing below this line is progress until something is deployed and somebody
outside this repo has opened it.

**Builder's gravity, named (R7 / session gate 5):** three stories built today and **zero
initiated user conversations** all week. BET.md needs 5 two-way exchanges by Aug 31 and has
0. Baseline items 1, 3 and 4 are done; item 2 (push) still needs an iOS shell that does not
exist, the App Store deadline is Aug 14, and nothing is deployed. Every line above is an
input metric. None of them is progress.

**What story 0013 actually cost, and what it did not buy (R7), 2026-08-13.** The pool was
never what was stopping this bet: 110 works is 3.6 months of dailies and the kill review is
in **eighteen days**. This moved none of BET.md's five numbers. The scoreboard is unchanged
at 0 live / 0 posts / 0 keywords / 0 conversations / 0 installs, the App Store live-by date
is **tomorrow**, and `config/deploy.yml` still carries two `CHANGEME` values.

What it did produce, beyond the count: five defects that only existed because something
checked. The quota table failed its own first run five works short and wrote nothing. The
first report that passed every bar was 46% South Asian — the range floor met by draining
one collection, which answers persona 3 with a different monoculture. 156 works counted as
"range" turned out to be Dutch and Flemish. 983 Minneapolis image URLs were well-formed and
every one of them 403'd. And pointing the existing fold-budget test at the new pool's worst
case put the day's first written line 193px below the fold on a 297-character title.

None of that is progress either. It is the difference between shipping 2,000 paintings and
shipping 2,000 rows.

| 2026-08-19 | **Story 0021 "The mock door" built: local dev can finally sign in behind `require_reader` without live Google/Apple credentials.** Apple can never be exercised locally regardless (cross-site POST callback, verified only on the deployed domain), and a fresh checkout has no real credentials either — every authenticated screen (`/corner`, `/collection`, the handoff claim) was untestable by hand since story 0015. Reuses `test_helper.rb`'s own mocking mechanism (`OmniAuth.config.test_mode` + `mock_auth`) for a human clicking around a running `rails s`, driving the real, unmodified `SessionsController#create` path — not a parallel sign-in mechanism. Dev-gated by a runtime config flag read through a routing **constraint**, not a boot-time `if Rails.env.development?`: the constraint form is what let one integration test flip the flag and drive the real route in `test` env, closing the gap a boot-time guard would have left permanently manual. `/plan-eng-review` (4 findings, 2 cross-model tensions against an outside voice — Codex rate-limited mid-review, fell back to a Claude subagent, which caught a real bug in the review's own proposed CSRF test before it shipped) → implement → `/simplify` → `/code-review`. **Code review caught the story's own P0**: the sign-in form had no `data: { turbo: false }`, so Turbo Drive (pinned and imported globally) intercepted the POST and refused to render `#create`'s non-redirect response — the door was completely inert to an actual click in a real browser. Neither the integration test (drives requests directly, never runs Turbo) nor this story's own curl-based manual verification (no JS engine) could have caught it; a new headless-Chrome system test can, confirmed red without the fix before being trusted green. Also added a defense-in-depth controller guard for the identity-minting endpoint, on top of the routing constraint, after code review argued a bad future routes.rb edit shouldn't be the only thing standing between this and production. **Renumbered from spec 0020 to 0021** mid-build — origin/main gained a different, concurrently-built story that also claimed "0020" (below) while this was in flight; a WIP=1 signal worth naming rather than quietly absorbing. `bin/ci` green: rubocop, brakeman, bundler-audit, importmap audit, 421 unit/integration + 60 system tests. **Code only, not deployed** — the route is inert in production by construction (`config.x.dev_sign_in_enabled` is `false` there), so a deploy would move nothing observable; skipped rather than bundled for its own sake. Zero `BET.md` thresholds moved: an input metric under R7. | ce4ebc3 |
| 2026-08-19 | **Story 0020 "Keep where you find it" built: favorites reach `/feed` and `/artists/:slug`, the two work-showing surfaces baseline item 4 had never touched.** Both share `paintings/_painting.html.erb`, which rendered a plate and a wall label and no keep control — a work the reader *found* could not be kept from where it was found. Renders inline, server-side, with no `src`: `/`'s fetching-frame architecture exists only because `/` is `Cache-Control: public` behind Thruster, and the walled surfaces have no shared cache to poison, so the real button ships from the first paint — one indexed query per page (`kept_ids_for`, now shared in `ApplicationController`) instead of one round trip per work. **Two corrections to the plan's own earlier drafts, both settled by reading source rather than asserting.** `Rack::ETag` already digests the response body on every 200 and skips only when an ETag is already set, so both walled pages already get a per-reader-correct ETag for free — deleted `stale?` and its `[count, latest]` aggregate from `ArtistsController` rather than extending it with a reader fingerprint (draft 1) or reaching for `no_store` (draft 2), both of which would have solved a problem the middleware already solves. Held by a mandatory regression test: keep a work, re-request with the prior ETag, expect 200 with the filled mark, never a stale 304 — and it genuinely failed red against the old code first. Second: `turbo-rails` intercepts a form submit only through its *closest* frame, so the feed's pagination frame (`target="_top"`) cannot swallow a nested keep tap into a full-page navigation — downgraded from a red-first gate the plan had ordered to a regression guard. **Mark-only on both surfaces, and the cost is written down, not assumed away**: dropping the `"N kept"` link narrows one of the two mitigations `decisions/0010` bought the label-less rail with, so that decision and `DESIGN.md` both now carry the narrowing and the original tripwire — a misread glyph in a `BET.md` conversation falsifies the call and labels are the costed fix. CSS reserve rescoped `.plate:has(+ .rail)`, not `.page:has(.rail)`: the old selector would have charged every plate on a page the moment one grew a rail, wrong on a 110-work scroll. **One test-writing gotcha found mid-build and worth keeping**: every post fades in from `opacity: 0` via its own `IntersectionObserver` (`reveal_controller.js`), and this driver treats that as not-visible, so system tests targeting a work past the first couple needed an explicit `scrollIntoView` — not a product defect, but it silently broke three new tests before the fix. Design review 5/10 → 9/10 (2 decisions); eng review 7 findings folded, 0 critical gaps, scope-challenge triggered and one reduction accepted. **Outside voice unavailable** — Codex hit its usage limit mid-run; no independent second opinion ran on either review. `bin/ci` green: rubocop 115 files 0 offenses, bundler-audit clean, importmap audit clean, brakeman 0 warnings, 418 unit/integration runs / 2131 assertions / 0 failures, 59 system runs / 0 failures. **Code only, not deployed, zero users, zero installs**: an input metric under R7. All five `BET.md` thresholds are unchanged and zero, twelve days from the Aug 31 kill review. | b9a6727 |
| 2026-08-20 | **Fixed: an artist-name tap past page 1 of `/feed` wrote "Content missing" over the whole page's worth of cards.** Reported live by the user, reproduced by forcing the lazy `feed-page-2` frame to load and clicking an artist link inside it. Root cause: `paintings/_page.html.erb`'s lazy next-page placeholder (`turbo_frame_tag "feed-page-#{next_page}", src: ..., loading: :lazy`) never carried `target: "_top"` — only the outer page-1 wrapper did. Turbo swaps a frame's fetched response into the element already in the DOM by replacing its *content*, not the element itself, so the placeholder's own attributes — set once, before the fetch — are what a click scopes against forever; the loaded response's matching frame tag (which DOES say `target="_top"`) never overwrites it. Every artist link on page 2+ therefore scoped its click to that frame instead of breaking out; `/artists/:slug` carries no `feed-page-N` frame in its response, so Turbo answered with the literal words "Content missing" in place of every card that frame held. Story 0020's own regression note tested the sibling case (a keep-button submit can't be swallowed by the *closest* frame) but never a link two frames deep on a page that had never had one before 0022 Release 1's infinite scroll existed at meaningful depth — the gap was old, the exposure was new. Fixed by putting `target: "_top"` on the placeholder tag directly. One regression test, confirmed red against the prior code (`Expected: "_top", Actual: nil`) then green: asserts the placeholder's own attribute, not a click, because the placeholder is the one chance the frame ever gets. `bin/ci` green: 465 unit/integration + 62 system tests. Live re-verified in the browser against the exact reported case (Berthe Morisot, `/feed` page 2) before and after. | 38f444d |
| 2026-08-20 | **Story 0025 "What the title says" built: the genre facet's second route, title-inference, takes coverage 12.4% → 36.5% (248 → 730 of 2,000 works).** Story 0022's museum-tag route was structurally blind to CMA and MIA — no subject field at any effort — leaving Amara's genre filter empty almost everywhere she browses and Maya's "paintings of flowers" autocomplete demand facing zero Flowers works. `Pool::TitleGenre.infer(title)` is deliberately **title-only**, not description — the 0008 probe measured the description route as noisy ("this painting may be a portrait of a favorite bird" is not a portrait) — a pure function, ordered `\b`-anchored pattern table, first match wins, same idiom as `Pool::PeriodBucket` and `Pool::Tradition`. Ladder stated once in `db/seeds.rb`: museum tag (0022) > title inference (0025) > nil, so `TitleGenre.backfill!` recomputes every row and a narrowed dictionary honestly retracts (verified live: the Lakshmi Bai fix retracted exactly 1 row on rerun). **Eng review folded 12 findings before implementation**, the load-bearing one a precedence audit run against real genre-nil titles, not assumed: Portrait must precede Mythological ("Portrait of Diana Mary Barker" is a person, not a myth), Mythological must precede Religious ("Diana or Artemis, Goddess of the Hunt"), Religious must precede Flowers ("Lotus Sutra with a Frontispiece" opens with a flower word but is a religious text) — three traps a flat dictionary would have hit blind. Flowers is a stated deviation from 0022's Getty-AAT-terms constraint: it's the reader's own word (0008 §3.3), not a controlled-vocabulary term, logged here rather than silently widening the precedent. **Audit: 98/100 on a weighted sample, gate passed (≥95%)** — two catches, a historical-figure title firing the `lakshmi` religious term (fixed with a `(?!\s+bai)` lookahead) and a borderline "Portrait of a Greyhound Dog" kept because the museum's own title calls it a portrait. Two more precedence traps surfaced only by running the audit against real titles beyond the plan's draft (Portrait > Mythological, Mythological > Religious, both above). Genre Scene, Cityscape and Animal Painting culled below the 5-work display floor at implement time; Nude ships at 7. Reconciliation STARVED flags on Flowers (21/148) and Landscape (51/210) are named as expected, not regressions — the 0008 probe's candidate ceiliings came from loose regexes, the shipped table anchors hard by design (precision over recall). `/qa` (live browser, real sign-in): 0 issues, 100/100 — genre facet options, Flowers filter correctness, facet-combination (genre+period), unknown-genre-slug graceful degradation, and the page-2/artist-link regression class (fixed 2026-08-20, commit 38f444d) all re-verified clean against a running server. `bin/ci` green: 556 unit/integration + 64 system tests, 0 rubocop/brakeman/bundler-audit/importmap warnings. **Code only, not deployed, zero users, zero installs**: an input metric under R7. Second of the three-story `decisions/0016` sequence; unlocks story 0026's pool expansion, which depends on both findability stories shipping first. | 97f5df3, 9693875, 21e2167 |
| 2026-08-20 | **Story 0026 "The wider pool" built: the curated art pool grows 2,000 → 2,300 works, closing thin-supply gaps decisions/0016 named** (Persian miniature, thangka, still life/flowers, marine, mythological, ukiyo-e, Madhubani, vanitas, icon, cityscape). Every one of the 2,000 pinned works ships unchanged — verbatim `feed_order` and `genre`, Jordan's contract enforced by 2 mandatory regression tests against a committed prior-manifest snapshot, not just assumed. **decisions/0016's own prediction 1 was falsified for real, on the record, not hypothetically**: `pool:curate` at the targeted 3,000 raised `Unmeetable` against the real museum mirrors (pool size 2,847/3,000, museum text 1,999/2,100). Root-caused by binary-searching the real additive ceiling: `MAX_REGION_SHARE=0.25` caps Europe/South Asia/East Asia at a quarter of the pool each — all three hit that cap simultaneously — and these four museums' CC0 holdings carry almost no North American painting (531 total, a hard supply ceiling, not a caps artifact). The math (`pool_size = 0.75·TARGET + min(607, 0.25·TARGET)`) only balances up to ≈2,428; shipped at **2,300** with margin, verified via a fast simulated-resolver harness (measured real plate-reachability rates per museum) before committing to the actual multi-hour network-bound run, rather than guessing blind twice. Reasoned through decisions/0016's own fallback clause ("swap within whatever total the bars allow"): a swap of *which* works are pinned can't move a supply-and-policy ceiling, so "accept the lower additive target" **is** the fallback, not a different path — no swap logic was built. New demand-driven curation stage (`Pool::ThemeTargets`, `Curator#fill_themes`) counts toward 10 theme quotas via the SAME functions that stamp the `/feed` facet at seed time, so "counted toward quota" and "lights the facet" can't structurally disagree — and that discipline caught two real, subtle findings before they shipped silently: **Ukiyo-e Painting isn't a culture-string match at all** (`ukiyo`/`floating world` fire on zero mirror candidates — museums catalogue by school and format, not the English label; matched instead via a curated artist allowlist + a medium-field check that keeps woodblock prints out, landing on 126 genuine hand-painted works across all four museums) and **Cityscape can't reuse Landscape's bare "View of X" pattern** (44 such titles exist, the large majority natural or river views — "View of Cotopaxi" is a volcano — reusing the blunt pattern would have mislabeled all 44; a curated city-name allowlist with a lookahead pattern catches the 12 genuine city views instead). **`/code-review`'s most severe finding caught a stale-report bug of exactly the shape its own design was built to prevent**: `pool_report.md`'s theme table is generated at curation time, before `pool:genre_fill` runs — so it claimed Icon and Vanitas both cleared their floor (5/5 each) when the true post-pipeline count, once the museums' own native tags legitimately outranked two title-inferred guesses (the seed ladder's own tag-over-title rule, correctly applied), was Icon 4 and Vanitas 2. Fixed by restructuring `Pool::ThemeTargets::TABLE` from an opaque precomputed matcher to `[name, kind, values, want]` — one declaration both the curation-time stage and a new post-pipeline recount (`Pool::Report.theme_recount_section`, wired into `pool:genre_fill`) derive from, so the two can never silently disagree again; the fix surfaced a fourth affected theme the stale table had also missed (Marine 12→11, still well clear of the display floor). Code review also caught and fixed a case-sensitivity gap in the new Ukiyo-e artist matcher and hardened the pinning mechanism against a latent (not-yet-observed) dedup collision. Live curation audit (100-work mechanical sample + full theme-bucket spot check) found one real defect: two Native American works catalogued by AIC under the tribal nation as maker ("Cheyenne", "Lakota") were shipping as live, misleading `/artists/:slug` pages — same shape as "China"/"Japan" already on `Painting::NOT_AN_ARTIST`, added. `/qa` (live browser, real sign-in): 0 issues, 100/100 — all 3 new facet values verified live (Cityscape, Madhubani Painting, Ukiyo-e Painting), facet combination, unknown-slug degradation, the page-2/deep-artist-link regression class, and Jordan's contract itself (feed_order 0 renders the identical painting before and after) all re-verified clean against the real 2,300-work pool. `bin/ci` green throughout: 586 unit/integration + 64 system tests, 0 rubocop/brakeman warnings. **Code + curated data only, not deployed** — Kamal deploy and the prod backfill are operator work (agent shell lacks secrets), additive-only by construction (the pin makes this safe: existing rows byte-identical, `db:seed`'s own idempotent upsert is the runner, no new migration). Zero `BET.md` thresholds moved: an input metric under R7, three days from the Aug 31 kill review, priced in decisions/0016 from the start. | bd2b82f, 9b0a06d, d398a58, 2c00376, e722d9a, b37dbbe, 51608b9 |
| 2026-08-19 | **Story 0023 "The standing order" built: the daily queue fills itself.** Every published day has required a same-week manual admin action since launch — CLAUDE.md's own Proven-baseline pipeline line ("curated queue → daily publish job") named this the day one entry was ever written, and `config/deploy.yml` had carried the IOU since first deploy. `DailyPick.auto_fill!` tops the queue 7 days ahead via a Solid Queue recurring job (05:00 America/New_York, after the app's own Eastern rollover), picking randomly with a 4-tier relaxation ladder (artist ±30d + culture adjacency → artist ±30d → artist ±7d → any eligible painting) so a day always fills without ever running out of rules. `auto_tier` stamps the row with which tier filled it — provenance and the audit instrument for decisions/0015's spacing prediction in one column, not a log line: Kamal replaces the container on every deploy, so a log-only receipt would not have survived to the kill review that reads it. **Solid Queue enters the stack**, pre-authorized by CLAUDE.md for exactly this job — runs inside Puma (`SOLID_QUEUE_IN_PUMA`), a second `queue` SQLite database on the same Kamal volume, `db:prepare` verified end-to-end against a real 2000-painting dev seed. **The buffer that protects the moat also deletes the failure detector that watched it**: a curator who used to notice a stale queue by opening the admin daily no longer has a reason to look, so `/queue-health` ships as the dead man's switch — public, no wall, `no-store`, 200/503 for an external uptime monitor — and the existing decisions/0004 museum-text banner splits its denominators (hand-picked days still measured against the original 20% bet; auto-picked days against decisions/0015's new one-third tripwire, binding at 30 auto-picked published days) so a correct system filling blurbless days by design doesn't read as a broken promise. Destroying a future machine pick is a re-roll, not a veto — the date refills at the next run, possibly with the same painting — and the flash says so at the exact moment a curator could otherwise be surprised. **Eng review folded 17 findings before a line of code shipped** (`auto` boolean → `auto_tier` integer for a durable audit; nil-artist-slug placeholders would have silently exempted the whole deny-listed cohort from spacing without the raw-string fallback; `SOLID_QUEUE_IN_PUMA: false` was already sitting in `deploy.yml` and had to be flipped with an explicit string-equality gate, not a truthy check). **Code review's cross-model adversarial pass caught what both eng review and QA missed**: the admin queue-depth hint and `/queue-health` computed "is the queue healthy" two different ways — the admin page checked buffer depth alone, never whether *today itself* had a pick, so a curator deleting today's published day would see a clean depth line while the front door served yesterday's painting and the external monitor was already firing 503. Fixed with one shared `DailyPick.queue_healthy?` predicate both surfaces call. Second: `fill_one!`'s `RecordNotUnique` rescue assumed any collision meant the *date* was taken, but `daily_picks` carries two unique indexes, not one — a `painting_id` race would have left a date silently unfilled while reporting success; now checks which constraint actually fired before deciding. That branch stays honestly untested: Rails validates uniqueness before every insert, so a single-connection test can only ever hit `RecordInvalid` first, and the real trigger needs two Solid Queue runs racing on separate connections, which this suite's transactional fixtures cannot construct — stated in a code comment rather than papered over with a misleading test. **One simplify-pass DRY win reverted on purpose**: sharing the NOT-IN-OR-NULL SQL builder across two call sites made Brakeman unable to statically prove the interpolated column was always a hardcoded constant; kept six duplicated lines rather than add a scanner suppression. QA against a real dev server (full 2000-painting seed, real HTTP Basic Auth, real destroy-and-reroll click) found zero bugs — every fix above came from code review, not from exercising the UI. `bin/ci` green: rubocop 0 offenses, bundler-audit clean, importmap audit clean, brakeman 0 warnings, 451 unit/integration runs / 2235 assertions / 0 failures, 59 system runs / 0 failures. **Outside voice ran on both reviews as a Claude subagent, not Codex** — usage-limited again today, the second story in a row this has happened; two of its findings were verified against source before being folded (`deploy.yml`'s pre-set `SOLID_QUEUE_IN_PUMA: false`, `artist_slug_for`'s nil return for every deny-listed placeholder) rather than trusted blind. **Code only, not deployed, zero users, zero installs**: an input metric under R7. Moves 0 of BET.md's 5 thresholds by itself — what it buys is the prerequisite half of the push moat (a guaranteed fresh day for a notification to point at) and the deletion of a daily operator tax, twelve days from the Aug 31 kill review. | fb67cf7 |
| 2026-08-20 | **Story 0024 "The named traditions" built — a third `/feed` facet, alongside period and genre, naming Mughal/Pahari/Rajput/Kalighat/Jain/Korean/Chinese/Japanese/Persian & Islamic/Tibetan & Nepalese painting.** First of the three-story `decisions/0016` sequence (findability → findability → expansion) commissioned off `user-research/0008`'s finding that ~35% of the pool carries a nameable tradition in free-text culture strings no facet could express — persona 3's verbatim ask ("Mughal miniatures, ukiyo-e"). `Pool::Tradition.from_strings` maps culture ∪ country ∪ department (plus artist, only when it's a `NOT_AN_ARTIST` placeholder — two of the ten table rows are unreachable any other way) through a `\b`-anchored, precedence-ordered pattern table onto the generic 0022 facet machinery. All 10 values clear `MIN_FACET_WORKS` at 19–276 works each; audit sample 49/50 correct (98%), the one miss a department-only Met org-chart artifact named as an accepted risk before it was found, then found exactly there. **Eng review (13 findings, 6 outside-voice) caught three real data facts by simulating the mapping table over the manifest at plan time**: the Jain bucket was dead on arrival without `gujarat` (1 work vs the intended ~51), 4 colonial-era Korean works would have mislabeled as Japanese without a precedence fix, and 12 works needed the placeholder-artist union to resolve at all. **`/simplify`'s four parallel review agents converged 3-for-3 on one root cause**: the controller/view's three-way hand-copied facet block was closing the exact bug class (`5d90908`, a filter silently dropped at lazy page 2) this story's own plan had just fixed once — now driven off `Painting::FACETS` so a fourth facet can't repeat it; `Pool::Tradition`'s backfill/audit logic moved off the rake task onto tested module methods. **`/code-review` (high) found the simplify pass's own new duplicate**: `facet_counts` ran twice per facet per request across `resolve_facet_slug`/`displayed_facet_values` — fixed with a `counts:` kwarg, the same duplicate-query shape story 0020 had already fixed once for `Painting.count`, not the cross-request caching question eng review had separately decided to leave alone. `/qa` (live browser, real sign-in): 0 issues, 100/100 — every filter combination, the page-2 regression, and unknown-slug graceful degradation verified against a running server, not just Minitest. `bin/ci` green throughout: 524 unit/integration + 63 system tests, 0 brakeman/rubocop warnings. **Code only, not deployed, zero users, zero installs**: an input metric under R7. Moves 0 of `BET.md`'s 5 thresholds; findability prerequisite for story 0026's pool expansion, eleven days from the Aug 31 kill review. | fb950f6, 4aed91b, 2695796, 4e8e697 |
| 2026-08-21 | **Story 0027 "The wing label" built: the `/feed` filter, owner-rated 3/11 on the Chesky scale (34 controls above the first work, 176 of 318 two-facet combinations dead), is now one glyph on the sticky rail.** Three in-flow facet rows (0022/0024) deleted outright — `_facet_row.html.erb`, `.facets` CSS, gone — replaced by a door that opens `/feed/index`: the wings as plates (a real work stands for each tradition/subject, uncropped), scoped counts (switching Mughal to Pahari is a replace, not an AND — `Painting.index_for` counts every facet within the OTHER active facets' scope, never its own), and a masthead label in the editorial voice (`Painting::FacetVoice`, hand-written short/sentence forms for every canonical value — "Mughal painting, eighteenth century", not "Mughal Painting, 18th Century"). Direction chosen from a three-round Claude Design canvas (round 1: three in-flow options, all rejected as still too much chrome; round 2: five zero-pixel-default doors; **F** — the door rides the rail, survives scrolling — picked, then **Plates** of three index treatments). Design review 6/10 → 9/10, 19 decisions (the native stack moves to `ios/Tondo/path-configuration.json`'s own modal-context rule rather than a `data-turbo-action` link attribute; the door renders unconditionally rather than costing `/feed` a query to check). **`MIN_FACET_WORKS` moves 5 → 16, not the story's own first guess of 20** (`decisions/0017`): the eng review's outside voice measured that 20 would have silently dropped Persian & Islamic painting at 19 works — the pool's second-most-searched tradition (`user-research/0008`) — while still culling every genuinely thin value the story named. Exact below-floor lists pinned per facet in `pool_quota_test.rb`, tradition AND genre both (a value starving or recovering fails loudly, `assert_equal` not `assert_empty`). Eng review: 9 findings + 12 outside-voice folded; scope reduced at D1 to one controller action (`PaintingsController#wings`, no new controller or concern) rather than the plan's first sketch. **`/simplify` (4 parallel agents) found real duplication `/code-review` then found MORE of, one layer deeper**: `Painting::FacetVoice` absorbed `FACET_LABEL` and the plate-vs-row facet split, both of which had leaked onto the controller with the VIEW reaching into a controller constant — an inverted layer; `_compass.html.erb`'s `door:` local changed from a hash the partial resolved into a route itself to a caller-supplied path string, since the partial renders on non-gallery screens too and had no business knowing `/feed/index` existed; one duplicate `COUNT(*)` removed from `#wings`. **`/code-review`'s top finding was a bug `/simplify`'s own reuse fix had just introduced, caught live in a browser**: joining `.plates__img` to the shared `.plate__img, .days__thumb img` CSS selector put the 80px height override BEFORE that shared rule in the file — equal specificity, so the LATER rule won the cascade, and every plate measured 131.78px instead of 80px (worse for the no-picture resting box, an empty span, which collapsed to a 2px sliver). Fixed by moving the override after the shared rule, same placement `.days__thumb img`'s own override already used two rules down for the identical reason — and a second instance of the SAME bug (`.plates__img--none`'s width) was caught by re-checking with a synthetic element after the first fix, not by the reviewer. Code review also caught two links (the coda's "Another wing", the empty state's "Open the index") that dropped the active filter instead of preserving it like the rail door does — both fixed, and preserving the filter on the AND-to-zero empty state turned out to be the MORE useful behaviour, not just the consistent one (it shows the reader the wing's real subjects instead of a blank index). `bin/ci` green throughout: 611 unit/integration + 66 system tests, 0 rubocop/brakeman/bundler-audit/importmap warnings. **Golden path browser-verified against the real 2,302-work dev pool**, not just Minitest: signed in via `/dev/sign_in`, drove door → index → tap Mughal → filtered feed (label "Mughal painting", aside "108 WORKS", door filled) → reopened index (Mughal marked current and dim, Subject scoped to Portraits alone, Century to 16th/17th/18th, coverage line "Subject is known for 24 of 108 works") → Done → scrolled to the walk's last page → coda "Every match, end to end." + "Another wing" → a real AND-to-zero combination (`tradition=korean-painting&genre=nude`) → the empty state, ornament, both doors, both preserving the filter. Every number matched manual DB queries taken before implementation. **Not performed: iOS simulator verification** — no Xcode access this session; the path-configuration modal-context change is unverified on-device, named as a TODO in the plan's own Deviations section rather than assumed. **Code only, not deployed, zero users, zero installs**: an input metric under R7. Moves 0 of `BET.md`'s 5 thresholds; ten days from the Aug 31 kill review. | 9e956ed |
| 2026-08-25 | **Submitted to App Store review — the first of BET.md's five thresholds is finally in motion, eleven days past its Aug 14 target.** Build `1.0 (1)` archived locally (real `TONDO_APP_SECRET` via `script/ios-secrets`, not the CHANGEME a cloud build would have shipped — Xcode Cloud got itself enabled during setup and its builds clone without the gitignored secret; its two green builds were bypassed, not used). Uploaded via Organizer, listing filed complete: name/subtitle per `decisions/0007`, Education + Reference, expect 12+ (nudity questionnaire answered honestly), keywords the 0016 plan's 100-char field, description updated 2,000 → 2,300 works to stay a checkable number, three 6.5"-resized frames (ASC's slot rejected the 6.9" 1320×2868 originals), privacy labels incl. the Diagnostics/Crash Data row the 0016 plan predates (Sentry, decisions/0018), sign-in-required unchecked (device auto-registers), manual release selected. **Status: Waiting for Review.** Top rejection risk named in advance: guideline 4.2 minimum-functionality — a chrome-less Hotwire shell with no push yet is the classic web-wrapper target; if it hits, the Resolution Center reply is same-day and story 0010 (push) becomes the resubmission fix. Not a threshold yet under R7 — "live on App Store" means approved AND manually released; four other thresholds still zero with six days to the kill review. | ASC: Tondo 1.0 (1), Waiting for Review |
| 2026-08-24 | **Gate-6 fast-path deployed and the backup receipt earned — decisions/0018's two open falsifiers both closed.** `e18a6d0` (Sentry + privacy disclosure + `script/backup-db`) went live in 118.8s: `/privacy` now carries the crash-reporting paragraph and the 21 August stamp, `/up` 200, `/` still `public, no-cache` + ETag with no `Set-Cookie`, `/queue-health` ok (scheduled through Aug 30). Then the backup ran for real: live `sqlite3 .backup` inside the container, pulled offsite to this machine, `PRAGMA integrity_check` **ok**, counts read from the pulled file — **paintings=2300, daily_picks=13, users=3** — remote copy removed after. The pull needed a detour: `gcloud compute ssh` died on `invalid_grant: Account has been deleted` (stale gcloud account, worth checking against project ownership), bypassed via Kamal's own `root@34.59.147.140` key; `script/backup-db`'s gcloud step is broken until that auth is fixed. The 2300 count is also the receipt that the 0026 pool expansion IS live in prod. File: `tmp/backups/backup-20260824T184901Z.sqlite3`. Zero `BET.md` thresholds move — this is submission-path clearing, seven days from the kill review. | e18a6d0 deploy, backup-20260824T184901Z.sqlite3 |
| 2026-08-21 | **Story 0027's own named TODO closed: iOS simulator verification, done for real.** The prior entry's "no Xcode access" was wrong — never checked; Xcode 26.5 was present the whole time. Built and ran the Debug scheme on an iPhone 17 simulator. First launch's device registration 401'd — a real, pre-existing bug found along the way, not a fixture issue: `Debug.xcconfig` never included `Secrets.xcconfig` the way `Release.xcconfig` does, so no Debug build could ever carry the Rails-credentials-matching `TONDO_APP_SECRET`, only the always-wrong dev literal. Fixed with one `#include? "Secrets.xcconfig"` line, mirroring Release's existing pattern; rebuilt, registration succeeded. With a registered device, drove door → `/feed/index` → `Done` on-device: `/feed/index` renders with rounded top corners and a dimmed status bar — the standard iOS sheet signature, confirming `path-configuration.json`'s `context: "modal"` rule is honored — and `Done` collapses cleanly back to the exact prior `/feed` scroll position, no stacked duplicate screen. Both open questions from 0027's Deviations section are now confirmed on real Hotwire Native navigation, not just JSON byte-equality. **Code only, not deployed.** | 2efd69b |
| 2026-08-27 | **Story 0010 "the single knock" (baseline item 2 — daily push) merged to main via PR #9, full build flow run end to end.** `decisions/0019` fixed the shape (noon ET, `apnotic`, `/you` opt-in, at-most-once). Eng review folded 16 findings before a line of code shipped. Implementation: `NativeEndpoint` concern, `PushRegistrationsController` (enroll/refresh modes so launch healing can never silently reverse a web opt-out; opt-out clears by token value, not row, so a device wipe's stale duplicate row stops getting knocked), `DailyPushJob` (claim-then-send, `push_sent_count` receipt), `/queue-health`'s 12:15 ET grace clause, `/you`'s version-gated toggle (`MARKETING_VERSION` → 1.1). `/qa` against a real dev server found the one bug that mattered: the opt-out button was silently non-functional on every real click — `head :no_content` is Turbo Drive's own "nothing to render" signal, so the DELETE cleared the token server-side while the stale ON button sat on screen forever; fixed by redirecting, the `DevicesController` idiom this controller should have used from the start. `/simplify` (4 parallel agents) then `/code-review` (12 findings, ~35 min multi-agent pass) together caught a HIGH-severity bug eng review missed: `DailyPushJob`'s credential lookup ran outside its own `begin/rescue`, so an incomplete `apns:` block would crash the job uncaught and permanently defeat the exact delivery tripwire `decisions/0019` was amended to add. Real editorial copy went in last, reviewed one field at a time rather than accepted in bulk — bare title/artist notification (**"reads like a museum wall label popping up, not marketing copy"**), plain `/you` toggle text. `bin/ci` green through every round (650 tests, 66 system tests, rubocop/brakeman/bundler-audit/importmap all clean) and a real `xcodebuild` for simulator succeeded after every iOS change. **Merged, not deployed, zero installs**: an input metric under R7. What it buys: the habit moat's first link exists in code, and the pre-named answer to guideline 4.2 if the 1.0 submission gets rejected for it. Still owner-gated before this reaches a reader: a physical-iPhone APNs delivery receipt, and a new App Store submission (1.1) — the build currently in review carries none of this. | PR #9, cc3da46 |
| 2026-08-27 | **Story 0028 "the overlooked nine" built and merged — a same-session "100 recognizable CC0 paintings" research artifact cut down to what this app can actually use.** Owner asked to add the artifact's 100 rows to local and prod; ~22 turned out to be Wikimedia-sourced (a named-and-rejected ingest source, `specs/0013`) and ~24 more came from museums this app doesn't mirror (NGA, Getty, Rijksmuseum, Smithsonian) — a real stack decision, not made here. Of what was left, 41 of 54 artist names were already in `user-research/data/0007-recognizable-names.json`. **The addressable gap was nine names**, added with real Wikidata QIDs, aliases and 12-month Wikipedia pageviews (same instruments story 0019's build script uses), not hand-typed. Re-running `pool:coverage`/`pool:curate` against the cached mirrors found **seven of nine were never actually missing** — the pool already held their work, invisible to coverage only because the names weren't on its list; the manifest came back byte-for-byte identical, 0 added, 0 removed. Two (George Catlin, Emanuel Leutze) are genuinely absent from the current mirror snapshot — Leutze's case traced to the Met's bulk CSV export apparently missing "Washington Crossing the Delaware" despite the live Met object API confirming it CC0 today, logged in `IDEAS.md` as a mirror-freshness lead rather than chased mid-story. `bin/ci` green (27/27 pool tests incl. the recognizable-coverage bar, full suite incl. 66 system tests). **Prod verified directly** via read-only `kamal app exec` rather than inferred from the manifest — matches local exactly, `TOTAL=2300` unchanged; nothing was deployed because nothing in the running app changed (`Pool::Recognizable` is a curation-time tool, never read at request time). No review requested — owner directed this story to skip design/eng review and QA. Moves 0 of `BET.md`'s 5 thresholds; four days from the Aug 31 kill review. | PR #10, eed4c41 |
| 2026-08-31 | **The Apple sign-in rejection fix, verified on-device against production — not just claimed.** The first fix (`d518ab3`, a 300ms defer) was the wrong theory. Installed a Release build on a physical iPhone (Dhanesh's iPhone, real Apple ID, `dailytondo.com`) and re-pulled prod logs mid-session: the retest showed `/session/handoff` hit **twice** with the byte-identical token, 300ms apart — `ASWebAuthenticationSession`'s completion handler firing twice for one sign-in, the second call losing the race against the single-use token (`spend_handoff_tokens!`) and bouncing to `corner_path` instead of the real destination. Fixed for real with a one-shot `handled` guard in `AuthRouteDecisionHandler.swift` so only the first firing ever routes (`f2b7208`). Rebuilt, reinstalled, retested: **exactly one** `/session/handoff` call this time (11:59:33 tap → 11:59:39 callback → single 303), reader confirmed landing correctly. **Still owner-gated: a new archive (build number bump), submission, and App Review approval** — this fix has never left this machine and this phone. | f2b7208 |
| 2026-08-31 | **Story 0030 "the permanent address" merged — every kept work gets a page, `/paintings/:id`, regardless of whether it was ever a Daily Pick.** Full build flow: `/plan-eng-review` (7 findings folded, self-review + Codex outside voice, 1 cross-model tension resolved on record), implementation with Minitest coverage, QA driven live in a real browser against the real dev pool (zero bugs found), `/simplify` (4 agents, 4 fixes), `/code-review` (10-angle multi-agent, zero correctness bugs, 6 findings folded — including catching a real bug in the review's own first-draft fix, a fixture-digest collision, before it shipped). `bin/ci` green throughout. **Merged via PR #13, not deployed**: an input metric under R7 — the fix for the Apple sign-in rejection two lines above it in this log is still the thing standing between this and a live app. | PR #13, 81f9258 |
| 2026-08-31 | **Kill review run, and the deadline moved — see `decisions/0020`.** All five `BET.md` thresholds read zero: not live (submitted Aug 25, rejected twice), 0 of 4 teardown posts, 0 of 3 ranked keywords, 0 of 5 user conversations, 0 of 50 installs. The bet's own falsification clause fires on the numbers — but the channel has never actually opened, so the bet has never been tested, only blocked on App Store review. Owner's call: extend one month, same five numbers, new kill review **Sep 30, 2026**. `BET.md` updated in place, Aug 31 scoreboard kept in it for the record rather than erased.
| 2026-08-31 | **App Store rejected the 1.0 (1) resubmission — guideline 2.1(a), "app did not proceed further after Sign in with Apple" (iPhone 17 Pro Max, iOS 26.6).** Pulled prod logs for the review window rather than guessing: the server-side flow completed clean three separate times (`/auth/start/apple` 200 → `/auth/apple` 302 → `/auth/apple/callback` 302 → `/session/handoff` 303, that last request arriving from the shell's own `Tondo iOS/1.0` WKWebView user agent) — proof the native completion handler fired and `navigator.route()` ran, ruling out credentials, the state-cookie CSRF fix, and the `omniauth.params` carry fix (all still doing their job). Narrowed to a client-side gap between the successful 303 and the screen updating: `ASWebAuthenticationSession`'s completion handler is documented to fire before its own sheet finishes dismissing, and routing into that transition can silently no-op. Applied the standard mitigation — a 300ms defer before `navigator.route(destination)` in `AuthRouteDecisionHandler.swift` — and confirmed it still builds clean via a real `xcodebuild` for simulator. **Unverified and said so**: no physical device or real Apple ID in this session to reproduce the stuck state or confirm the fix actually closes it. Owner-gated before this reaches App Review again: on-device smoke against a real Apple ID, a new archive, and resubmission. | d518ab3 |
| 2026-08-28 | **Story 0029 "one of each" built and merged — `/feed`'s browse order goes from a fixed shuffle to a composed sequence.** New `pool:interleave` rake task + `Pool::FeedInterleave`: round-robin across every genre/tradition/period value plus `untagged`, priority-claimed so a work counts once. `/plan-eng-review` (self-review + Codex outside voice) caught the plan's core data-source claim was wrong before a line of code shipped — `period`/`tradition` are derived at seed time, never literal manifest keys — and an internal manual re-run caught that the shuffle isn't idempotent across re-ordered input, confirming `pool:interleave` really is a true one-time migration, not a rerunnable one. Ran it for real against the committed 2,340-work manifest; regenerated `test/fixtures/files/0026-prior-manifest-snapshot.json` (required — the existing MANDATORY `pool_quota_test.rb` regression test would otherwise ship red). `/simplify` (4 parallel agents, fixes applied) and `/code-review` (zero findings) both ran clean. `bin/ci` green (663 tests, rubocop/brakeman/bundler-audit/importmap clean). **Merged, not deployed, zero installs**: an input metric under R7. Moves 0 of `BET.md`'s 5 thresholds; three days from the Aug 31 kill review. | PR #12, a1f85e2 |
| 2026-08-31 | **Story 0031 "out the door" merged — the share button, the shell's first bridge component (`decisions/0021`).** Share slot LEFT of Keep on every keep surface — including `/paintings/:id`, a FIFTH surface the story's "all four" never named: it reuses `paintings/_painting` verbatim, the control travels with the partial; recorded in the plan, not silently absorbed. Payload = full-res image + text + `root_url(via: "share")` — and eng review E1's wall audit survived story 0030 merging `paintings#show` under it, because the new page is walled like the artist page; the plan carries the re-check rather than a stale premise. Reveal is gated by the Hotwire Native bridge handshake read from the UA pre-paint (split-and-match on the `bridge-components` bracket list): old binaries never register `share`, so `/` and `/days/:date` stay public-cache-safe with zero server-side UA branching. MARKETING_VERSION 1.1 → 1.2. **Renumbered 0030 → 0031 at commit time** — three concurrent claims on "0030" (the permanent address merged first and keeps the number; "the standing bar" renumbers to 0032 when its branch is picked up) and the bridge decision entry moved 0020 → 0021 for the same collision. Second renumber-on-merge in this log (story 0021 was the first) — a WIP=1 signal named, not absorbed. `bin/ci` green post-rebase: 72 system runs incl. 6 new share tests, all stages clean. **Merged, not device-verified, not deployed, zero installs** — an input metric under R7. Owner-gated before any reader sees it: LAN device QA (plan step 7), TestFlight 1.2 upload, kamal deploy — and the 1.0 App Review approval that is still the gate on everything. | PR #14, 5cd4f6e |
| 2026-08-31 | **Story 0032 "sit with it" built and merged — the front door's note now waits behind a soft minute.** The full build flow ran in one day: brainstorm → IDEAS → story with intake fields → plan → `/plan-design-review` (12 decisions, outside voice, 5/10 → 9/10) → `/plan-eng-review` (13 findings, 4 outside-voice P1s — device-identity branch, expand-inside-details, describedby leak, no-src frame) → implement with tests as they went → live-browser QA against the real dev pool (one guard found: NaN duration = instant ready) → `/simplify` + `/code-review` inline (3 cleanups, 2 verified findings, and the review's own first C6 draft caught by the re-run suite before it shipped: it rendered the field above an open note — writing after reading — and re-broke the fold bound the summary-hiding had just fixed). What shipped: a native `<details>` gate on `/` only (archive and preview untouched), one server-rendered invitation that doubles as the reveal control, an invisible minute ending in a dim-to-gold ready state, an account-backed write-once `Impression` line that sits above the curator's note, an identity-free `sit_counters` beacon making `decisions/0022`'s ≥20% prediction measurable from day one, and a rule-8 amendment in `DESIGN.md`. **Owner-gated before ship is real: the invitation, ready line, field label, and error copy are DRAFT — the field-by-field pass is owed (T5).** `bin/ci` green twice consecutively (84 system runs / 459 assertions, rubocop/brakeman/audits clean). **Code only, not deployed, zero installs** — an input metric under R7; the 1.0 App Review approval still gates everything, and the beacon's number is honestly spoofable (recorded in the plan, not discovered later). | 23de2b2, b296a3e, 9d6bfa0 |
| 2026-08-31 | **Story 0032's own owed item closed same day: the copy pass ran, one field at a time, and every reader-facing word on the gate is now the owner's.** Invitation ("Please take a minute to look closer. The note is here whenever you are ready."), ready line (draft kept), field label ("Record your reaction. Even one line will do."), button ("Save" — "Save note" flagged for colliding with THE note, owner chose the bare verb), and the plain error pair, rendered without Rails' "Body" attribute prefix. One backstop string ("Already saved.", unreachable via UI) remains mine and is flagged for the owner. `bin/ci` green. Remaining before a reader sees any of it: deploy + the 1.0 approval. | 55e7ec9 |
| 2026-09-01 | **Story 0032 redesigned in place before it ever deployed: the prompt replaces the minute.** A day of the owner dogfooding the shipped gate produced three design rounds on a canvas (coda-beat/wall-label → LOOK·RECORD·READ ladder/row → the chosen shape: **one looking prompt, an answer line, and the skip**) and a same-day rebuild. The day's prompt now rotates from the owner's own protocol set (`DailyPick::SIT_PROMPTS`, P1/P2/P3/P5 + the first-impression move, `user-research/0010`) and carries the whole scaffold — it works for readers who never type; signed-in readers answer on an autosaving hairline field with no Save button (owner decision; draft until the reveal makes it ink — `decisions/0022` amended on the record), and `impressions.prompt` stamps which prompt produced the writing, turning the protocol's seed-order question into a table read. READ THE NOTE is gold from first paint and never locks. The timer, ready state, and aria-live machinery came OUT — the diff deletes more gate JS than it adds. Sep 1 itself found three archive tests hardcoding a one-month calendar and the suite's two documented favorites flakes started blocking CI — all five fixed, the compass one by its own IDEAS.md prescription. `bin/ci` green four consecutive runs; the full anonymous/signed-in/reveal flow verified live in a real browser. **Prompt copy is DRAFT — the owner's field-by-field pass is owed again.** Code only, not deployed, zero installs — an input metric under R7. | 7068668 |
| 2026-09-01 | **Deployed to production — the biggest single deploy this product has taken.** `dailytondo.com` now serves everything merged since Aug 26: story 0030 (`/paintings/:id`, every kept work reachable), story 0031's web half (the share slot, inert until a 1.2 binary's handshake reveals it), and story 0032 in its redesigned form — the front door greets every visitor with the day's looking prompt and the note folded behind READ THE NOTE. **Receipt, observed live, not claimed:** `/up` 200; `/` answers `public, no-cache` + ETag with no `Set-Cookie` and carries today's prompt ("Describe what you see to someone who cannot see it.") and the folded gate; `/feed` still 303s a stranger; the anonymous impression fragment answers 200. Migrations (impressions + prompt, sit_counters) rode `db:prepare` on boot. The `sit_counters` tallies and `impressions.prompt` start accruing from this hour. Also today: the 1.2 dev build was installed and launched on the owner's physical iPhone — the share glyph's first appearance on real glass. **Still owed:** the owner's pass on the five DRAFT prompt lines, the share sheet's on-device QA (`specs/0031` plan step 7), and TestFlight 1.2 — the App Store 1.0 approval remains the gate on every BET number. | 7068668 live at https://dailytondo.com |
| 2026-09-01 | **Story 0033 "whose words" built and merged — the sit gate's pinned note becomes a comment thread, the reader's own line sitting beside it instead of stacked above it.** A bubble glyph joins the rail, open by default, toggling the whole conversation; the museum/curator note is a pinned, folded comment (one tap to expand, never persisted); the reader's answer renders as their own comment — composer until answered, tap-to-edit after — replacing the old prompt-then-caption flow. Keep-count text drops from the rail with it (`decisions/0010` reversal, `decisions/0023`). Direction settled via three mock rounds (label-side treatments → comment-thread variants → a clickable prototype, iterated twice) and picked by the owner against two alternatives on the board. `/` stays `public, no-cache`, byte-identical, zero `Set-Cookie` throughout — the thread shell and pinned comment are cached markup, only the composer/comment is the per-visitor frame. **`/qa` against a live dev server caught what the suite structurally could not:** every real save 422'd — the autosave controller's write path switched from `FormData` to `URLSearchParams` during the tap-to-edit rework and dropped the CSRF token that came free with the old form-based post, invisible to `bin/rails test` because `config/environments/test.rb` disables forgery protection suite-wide. Fixed by reading the token from the composer's own walled-frame form; a forgery-protection-ON regression test (`sit_test.rb`, "the answer saves with forgery protection genuinely on") confirmed red without the fix before being kept. Code review: 15 findings triaged same day. `bin/ci` green. **Merged via PR #15 (`1e1ebda`), not deployed** — an input metric under R7; this SHIPLOG entry itself was the owed receipt, written same-session as story 0034 below after the gap was flagged at session start. | PR #15, 1e1ebda |
| 2026-09-02 | **Story 0034 "a frame for the doors" built and merged — `/you`'s four flat-text controls (Sign out, Delete account, Delete device, the notification toggle) become bordered chips, in two variants.** Trigger: a live-production screenshot and direct complaint ("these are just text and unusable to the user") — a repeat of ISSUE-002's affordance question from 2026-08-17, which fixed spacing but kept flat gold caps-link text as house style. This time the call reverses (`decisions/0024`, formally supersedes the `specs/0015-the-two-keys` plan's original stance): extends `.signin__door`'s existing chip anatomy (shipped since story 0015, zero complaints in a year) to the four flat controls — safe variant (hairline/gold, matching `.signin__door`'s border/background) and a destructive variant (`--warn` tint) for the two irreversible actions. `:active` press feedback added on top of the approved mock — hover barely fires on a touch-first WKWebView product. CSS-only: zero view/controller/model changes; `corners_test.rb`'s 22 tests (unmodified) are the regression guard. Full flow ran: mocks approved directly by the owner (Claude Design canvas), `/plan-design-review` (6→9/10, 3 findings folded), `/plan-eng-review` (clean; Codex rate-limited, independent Claude subagent caught a real contrast miscalculation and a documented WebKit `:active`-on-`<a>` caveat), live-browser QA (Chrome automation against a real dev server + a manually-registered device — matched the mock exactly, zero console errors, mobile viewport clean), `/simplify` (2 independent agents converged on the same finding — `--warn`'s RGB channels hand-duplicated 4×; fixed with a `--warn-rgb` custom property), `/code-review` (7 findings, 5 fixed including the one with real teeth: the destructive chip's `:active` fill computed to 4.54:1 contrast — technically AA, no margin, on the exact state a reader sees mid-tap on an irreversible action; dropped to 4.82:1). Two findings skipped and named rather than fixed: a reusable destructive-chip class (no second use case exists — infrastructure for later) and CSS relative-color syntax over the custom property (would relitigate a first-use-of-new-technique call already settled twice). `bin/ci` green throughout every round; the suite's one failure across the whole session (`sit_test.rb`) confirmed pre-existing and unrelated, independently, twice. **Code only, not deployed, zero installs** — an input metric under R7; the App Store 1.0 approval remains the gate on every BET number. | 3e23ead, ef2d24c, bd7dbfc |
| 2026-09-03 | **`/you`'s account state: a spacing bug and two copy fixes, deployed same day.** Trigger: a live-production screenshot. `.corner__doors` set `column-gap` only — Sign Out and Delete Account wrap to stacked rows on a narrow phone or under Dynamic Type and landed touching border-to-border with no gap between them; changed to `gap`, both axes. Copy: `signed_in_summary` assumed Apple's Hide My Email leaves the email field blank — it actually hands over a real `@privaterelay.appleid.com` forwarding address, so the identity line printed that gibberish back as if confirming it; now suppressed like the no-email case (shared by `/you` and `/collection`, test coverage added). "Nothing else lives here yet" sat directly under the kept-works count and read as contradicting it; reworded to "No other settings live here yet." **Receipt, observed live:** `/up` 200; deployed CSS carries `gap: 1.4rem` (not the old `column-gap`); prompted, screenshotted, and read against a real dev-server sign-in before shipping. `bin/ci` green (`sit_test.rb`'s two flakes confirmed pre-existing on unmodified `main`, unrelated). | 9afd0f9, 19ab5aa |
| 2026-09-03 | **Same session, second pass on `/you`: the page's explanatory copy trimmed and its dead top space scoped away — `decisions/0025`.** Four states mocked on a design canvas (iterated live with the owner over several rounds — link in the decision), then implemented: `:account` drops the kept-count/"no other settings" sentences to just "Signed in with Apple."; `:device` collapses its sign-in paragraph to one line; the push section drops both redundant on/off status lines. `.page--empty`'s 4-8rem top pad and `.ornament`'s flanking dashes are DESIGN.md-documented and shared by ten-plus other screens (404, empty archive/gallery/days, artists, daily, privacy/support) — reversed for this page only via a new `.corner` scope class, not a shared-class edit. `/code-review` (5 parallel agents) caught one real bug before ship — the new one-line device CTA hardcoded plural "works" regardless of `@kept_count`, wrong at exactly the count the renamed test's fixture used; fixed to agree, with singular AND plural coverage added. The same pass also flagged that comments referencing the cut consequence-copy had gone stale; updated in place rather than left to mislead the next reader. One process note for the record: a `/simplify` review fork, asked only to report findings, instead directly edited the working tree (scoped the CSS itself, merged a duplicated push-button wrapper) — verified correct and kept, but noted as a deviation from what was asked. `bin/ci` green; `sit_test.rb`'s flakes reconfirmed pre-existing, a third time this session. **Merged, not yet deployed.** | 1242b8c |
| 2026-09-03 | **Same session, third pass: a coverage gap closed on 1242b8c, found by a second Claude session running its own `/code-review` against this repo concurrently.** That session's finder caught what mine missed — the new integration test asserting `.corner` on `<main>` calls itself "the forcing function" against the shared-class regression, but `assert_select` reads markup, not the cascade, so a typo'd or deleted selector would leave it green. Coordinated live (agent-to-agent, both sessions on the same tree, one commit apart) rather than duplicating work; the other session also independently confirmed the copy cuts in `decisions/0025` were deliberate, not oversights, before reporting. Closed with a real system test (`test/system/corners_test.rb`, `getComputedStyle`) asserting the actual padding-top and dash-free ornament, and the integration test's comment corrected to stop overclaiming. `bin/ci` green (93 system tests now, +1). **Merged, not yet deployed.** | 424316c |
