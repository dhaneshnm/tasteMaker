<% content_for :title, "Tondo" %>

<%# `linked: false` — there is no archive to go back to yet, so the brand is
    not a door. test/integration/brand_test.rb asserts this stays a span.

    The compass renders anyway. The archive and the collection are both empty at
    this point and both have written empty states of their own saying so, which
    is a better answer than three special cases here. %>
<%= render "shared/masthead", label: "Artwork of the Day", linked: false, here: :today %>

<main class="page page--empty">
  <div class="ornament" aria-hidden="true">✦</div>
  <p class="coda__line">The first artwork arrives soon.</p>
</main>
